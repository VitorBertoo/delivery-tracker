spring.application.name=delivery-tracker-backend

# SQLite datasource
spring.datasource.url=jdbc:sqlite:delivery-tracker.db
spring.datasource.driver-class-name=org.sqlite.JDBC

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT — change the secret before going to production
jwt.secret=dGhpcy1pcy1hLXZlcnktc2VjcmV0LWtleS1mb3ItZGVsaXZlcnktdHJhY2tlcg==
jwt.expiration=86400000

# OSRM — swap for a self-hosted instance in production
osrm.base-url=http://router.project-osrm.org
